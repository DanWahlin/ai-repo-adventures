# LLM Configuration - Choose your provider

# === Azure AI Foundry ===
REPO_ADV_LLM_API_KEY=your_azure_key_here
REPO_ADV_LLM_BASE_URL=https://your-resource.openai.azure.com
REPO_ADV_LLM_MODEL=gpt-4o
REPO_ADV_LLM_API_VERSION=2025-01-01-preview

# === OpenAI API ===
# OPENAI_API_KEY=your_openai_key_here
# REPO_ADV_LLM_BASE_URL=https://api.openai.com/v1
# REPO_ADV_LLM_MODEL=gpt-4o

# === Ollama (limited but can be used to experiment)===
# If you use this you'll probably need to extend the LLM_REQUEST_TIMEOUT value significantly
# 300000 (5 minutes) or more depending upon your hardware
# REPO_ADV_LLM_BASE_URL=http://localhost:11434/v1
# REPO_ADV_LLM_API_KEY=ollama
# REPO_ADV_LLM_MODEL=gemma3:27b

# === GitHub Models (Free tier available but VERY limited and will only work with very small scenarios) ===
# GITHUB_TOKEN=your_github_token_here
# REPO_ADV_LLM_BASE_URL=https://models.inference.ai.azure.com
# REPO_ADV_LLM_MODEL=gpt-4o

# LLM Parameters (Optional - defaults provided)
REPO_ADV_LLM_TEMPERATURE=1.0      # Temperature for LLM requests (0.1-2.0, default: 1.0, GPT-5 uses 1.0 regardless)
REPO_ADV_LLM_MAX_TOKENS=10000      # Default max tokens for LLM responses (default: 8000)

# GPT-5 Specific Parameters (Optional - only used for gpt-5, gpt-5-mini models)
REPO_ADV_GPT5_VERBOSITY=medium         # Response verbosity: low/medium/high (default: medium)
REPO_ADV_GPT5_REASONING_EFFORT=minimal  # Reasoning effort: minimal/medium/high (default: medium)

# Timeout Settings (Optional - defaults provided)
REPO_ADV_LLM_REQUEST_TIMEOUT=60000        # LLM request timeout in milliseconds (default: 60 seconds, increase for slow models like Ollama)
REPO_ADV_FILE_READ_TIMEOUT=10000          # File read timeout in milliseconds (default: 10 seconds)
REPO_ADV_FILE_ANALYSIS_TIMEOUT=30000      # File analysis timeout in milliseconds (default: 30 seconds)
REPO_ADV_LLM_CACHE_TTL=300000            # LLM cache TTL in milliseconds (default: 5 minutes)
REPO_ADV_REPOMIX_CACHE_TTL=60000         # Repomix cache TTL in milliseconds (default: 60 seconds)
REPO_ADV_REPOMIX_SUBPROCESS_TIMEOUT=120000 # Repomix subprocess timeout in milliseconds (default: 2 minutes)
REPO_ADV_REPOMIX_GRACEFUL_TIMEOUT=5000    # Repomix graceful shutdown timeout in milliseconds (default: 5 seconds)
REPO_ADV_REPOMIX_MAX_BUFFER_SIZE=104857600 # Repomix max buffer size in bytes (default: 100MB)

# Token Management and Chunking Configuration (Optional - defaults provided)
REPO_ADV_MAX_CONTEXT_TOKENS=128000              # Maximum context window tokens (default: 128000)
REPO_ADV_ESTIMATED_TOKENS_PER_CHAR=0.25         # Estimated tokens per character (default: 0.25)
REPO_ADV_CHUNKING_RESPONSE_TOKENS=10000         # Reserved tokens for LLM response (default: 10000)
REPO_ADV_CHUNKING_PROMPT_TOKENS=3000            # Reserved tokens for prompt template (default: 3000)
REPO_ADV_CHUNKING_CONTEXT_SUMMARY_TOKENS=8000   # Reserved tokens for context summary (default: 8000)

# LLM Client Throttling Configuration (Optional - defaults provided)
REPO_ADV_LLM_INITIAL_THROTTLE_DELAY=1000   # Initial throttle delay in ms (default: 1000)
REPO_ADV_LLM_MAX_THROTTLE_DELAY=30000      # Maximum throttle delay in ms (default: 30000)
REPO_ADV_LLM_THROTTLE_DECAY_RATE=0.8       # Rate to reduce delay on success (default: 0.8)

# Rate Limit Configuration (Optional - defaults provided)
REPO_ADV_TOKEN_RATE_WINDOW_SECONDS=60      # Token rate limit window for Azure S0 tier in seconds (default: 60)

# Input Validation Limits (Optional - defaults provided)
REPO_ADV_VALIDATION_MAX_CHOICE_LENGTH=200    # Maximum choice input length (default: 200)
REPO_ADV_VALIDATION_MAX_PATH_LENGTH=500      # Maximum path input length (default: 500)
REPO_ADV_VALIDATION_MAX_DISPLAY_LENGTH=1000  # Maximum display length (default: 1000)

# Multi-Model Testing Configuration (Optional - for integration tests)
# Comma-separated list of models to test for response compatibility
# If not set, integration tests only use the primary REPO_ADV_LLM_MODEL
REPO_ADV_LLM_TEST_MODELS=gpt-4o,gpt-4o-mini  # Models to test in multi-model integration tests

# Enable/disable multi-model testing (default: false to save costs in CI)
# Set to 'true' to run comprehensive multi-model compatibility tests
REPO_ADV_LLM_MULTI_MODEL_TESTS=false         # Run multi-model tests (expensive, use before releases)